[project]
name = "cn-geo-data-pipeline"
version = "0.1.0"
description = "Reproducible cleaning and analytics pipeline for the CN-GEO citation dataset"
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.5,<2",
  "numpy>=2,<3",
]

[tool.uv]
package = false
